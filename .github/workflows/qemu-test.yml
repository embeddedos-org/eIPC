name: EoSim Platform CLI Validation

on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:

env:
  # Install an existing immutable EoSim release tag directly from source.
  EOSIM_VERSION: "3.0.1"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  simulate:
    name: EoSim run smoke check (${{ matrix.platform }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        platform:
          - x86_64-linux
          - arm64-linux
          - riscv64-linux
          - stm32f4
          - raspi4
          - vexpress-a9
          - sifive_u
          - qemu-q35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install EoSim from source
        run: |
          git clone --depth 1 --branch v${{ env.EOSIM_VERSION }} https://github.com/embeddedos-org/EoSim.git /tmp/eosim
          pip install -e /tmp/eosim
      - name: Load platform registry
        run: eosim list
      - name: Exercise EoSim run command (${{ matrix.platform }})
        run: |
          echo "=== EoSim run-command smoke check: ${{ matrix.platform }} ==="
          eosim run ${{ matrix.platform }} --headless --timeout 15
          echo "${{ matrix.platform }}: eosim run exited successfully (execution may be a dry run)"
      - name: Collect artifacts
        if: always()
        run: |
          mkdir -p out
          eosim info ${{ matrix.platform }} > out/${{ matrix.platform }}-info.txt 2>&1 || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: eosim-validation-${{ matrix.platform }}
          path: out/
          retention-days: 7

  cross-platform-verify:
    name: Cross-Platform Verify (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install EoSim
        run: |
          git clone --depth 1 --branch v${{ env.EOSIM_VERSION }} https://github.com/embeddedos-org/EoSim.git "${{ runner.temp }}/eosim"
          pip install -e "${{ runner.temp }}/eosim"
      - name: Exercise EoSim registry and doctor commands
        run: |
          eosim list
          eosim doctor

  sanity-gate:
    name: EoSim Platform Validation Gate
    if: always()
    needs: [simulate, cross-platform-verify]
    runs-on: ubuntu-latest
    steps:
      - name: Results
        run: |
          echo "════════════════════════════════════════════"
          echo "  EoSim CLI Validation Results"
          echo "════════════════════════════════════════════"
          echo "Run-command smoke checks (8):  ${{ needs.simulate.result }}"
          echo "Cross-Platform (Win/Lin/Mac):  ${{ needs.cross-platform-verify.result }}"
          echo "════════════════════════════════════════════"
          if [ "${{ needs.simulate.result }}" != "success" ]; then
            echo "❌ EoSim run-command validation failed"; exit 1
          fi
          if [ "${{ needs.cross-platform-verify.result }}" != "success" ]; then
            echo "❌ Cross-platform verification failed"; exit 1
          fi
          echo "✅ All EoSim CLI validation checks passed"
